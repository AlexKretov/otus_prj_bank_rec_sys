# Code Review: banking_rec_sys_prj

Дата ревью: 15.09.2026. Объект: ветка `main` (коммит `ac17e73`).
Принцип рекомендаций: ничего не переписываем с нуля — точечно чиним и улучшаем то, что есть.

---

## 0. Резюме

**Что уже хорошо:**
- Завершённый сквозной пайплайн: EDA → предобработка → ALS-признак → sklearn Pipeline (OHE + scaler + feature selection + RF) → тюнинг Optuna → логирование в MLflow → FastAPI-сервис → нагрузочный прогон с отчётом.
- Осмысленный EDA с выводами после каждого раздела и итоговым аналитическим документом (`recommendations_analysis.md`).
- Все зависимости запинаны; `.env` и `data/` вынесены в `.gitignore`; секретов в репозитории нет (проверено).
- Есть артефакты для воспроизводимости предобработки: `replacer.json`, `columns.txt`, `feature_importances.csv`.

**Главные риски (по убыванию важности):**
1. Репозиторий в текущем виде **не собирается**: `requirements.txt` содержит маркеры merge-конфликта, `loader.ipynb` — невалидный JSON по той же причине, README задублирован.
2. Продовый сервис `app1.py` **не применяет часть предобработки** (результат `.map()` выбрасывается) и **не подтягивает персональные ALS-рекомендации** (lookup по позиционному индексу) — самый важный признак модели на проде фактически всегда `'0'`.
3. Целевая переменная сформирована **только по 3 продуктам из 24** (`range(3)` в `modeling.ipynb`), итоговые метрики в выводах **завышены** (precision 0.97 — это взвешенная метрика по классу «ничего не купил»; у целевого класса покупки precision 0.15).
4. Тюнинг гиперпараметров выполняется **по тестовой выборке** — заявленный ROC-AUC 0.9685 получен на тех же данных, на которых выбирались гиперпараметры.
5. `app.py` в закоммиченном виде **не запускается** (NameError, отсутствующий импорт, порча DataFrame).

---

## 1. Блокеры (чинить в первую очередь)

### 1.1. `requirements.txt` — неразрешённый merge-конфликт
Строки 1, 17, 33 содержат `<<<<<<< HEAD / ======= / >>>>>>> 18625...`. `pip install -r requirements.txt` падает сразу.
- Удалить маркеры, оставить один вариант (обе половины идентичны + `featuretools==1.31.0`).
- Заменить `load_dotenv==0.1.0` на `python-dotenv==1.0.1`: код везде делает `from dotenv import load_dotenv` — это пакет `python-dotenv`. Пакет `load-dotenv` (0.1.0) — другой проект, он только автозагружает `.env` при выставленной переменной окружения `LOAD_DOTENV` и нужного импорта не даёт.
- Добавить недостающие зависимости, реально используемые кодом: `fastapi`, `uvicorn`, `pyarrow` (чтение parquet), `woodwork` (прямой импорт в `app1.py`/`app.py`/ноутбуках; сейчас приезжает транзитивно с featuretools — лучше закрепить явно).
- Указать версию Python в README (numpy 2.2.5 требует ≥ 3.10).
- Опционально: пересмотреть жёсткие пины `mlflow==2.7.1` (2023 г.) и экзотичный `fireducks==1.2.8` (в коде не используется; если не нужен для FireDucks-ускорения pandas локально — убрать в `requirements-dev.txt`).

### 1.2. `loader.ipynb` — битый JSON
Маркеры конфликта на строках 5, 406, 411, 435–439 делают файл нечитаемым для Jupyter (проверено: `json.load` падает).
- Разрешить конфликт (выбрать одну сторону — они расходятся только в outputs).
- Заодно: `df.to_csv(r'data\train_ver2.csv')` → `df.to_csv('data/train_ver2.csv', index=False)` — уберёт необходимость в блоках `try: drop('Unnamed: 0')` в остальных ноутбуках и Windows-сепаратор пути.

### 1.3. README — задублирован и содержит маркер конфликта
Строка 73 — `=======`, весь документ повторён дважды (вторая копия отличается только пунктом про `loader.ipynb` и оформлением блока docker).
- Оставить одну (вторую) версию, удалить маркер.
- **Ссылки на несуществующие файлы**: `fastapi/app2.py` (фактически в репозитории `app.py` и `app1.py` в корне), `run_mlflow_server.sh`, `.env` (нет ни файла, ни `.env.example`), `fastapi/Dockerfile`, `docker-compose.yml`, `graphana.json`. Либо закоммитить эти файлы (если существуют локально — судя по `image.png`, инфраструктура реально поднималась), либо честно переписать раздел «Развёртывание» под текущее состояние: `uvicorn app1:app --port 8079`, мониторинг — «в работе».
- Опечатка в команде: `pip install - r requirements.txt` → `pip install -r requirements.txt`.

### 1.4. `app1.py:214` — редкие категории не заменяются
```python
for col in cats[:-24]:
    rf[col].map(replacer[col])   # результат не присваивается — no-op
```
Весь механизм сворачивания редких категорий на проде не работает (train/serve skew). Заменить на:
```python
rf[col] = rf[col].replace(replacer[col])
```
Именно `replace`, а не `map`: словарь содержит только редкие значения → `map` превратил бы все частотные значения в NaN.

### 1.5. `app1.py:229` — персональные рекомендации не находятся
```python
recommended_product_id = personal_recs.loc[rf['ncodpers'],'recommended_product_id']
```
`personal_als.parquet` сохранён в `modeling.ipynb` (ячейка 35) без установки индекса по клиенту (`to_parquet` с обычным RangeIndex). Значит `.loc[ncodpers]` ищет **номер строки**, а не ID клиента: почти для всех запросов — KeyError → голый `except` → константа `0`. При этом `recommended_product_id` — самый важный признак модели (`feature_importances.csv`: 0.108 + 0.040 + 0.010 сверху списка). На проде он фактически отключён.
- При сохранении: `recommended_tracks_df.set_index('ncodpers').to_parquet(...)` (или `pd.read_parquet(...).set_index('ncodpers')` при загрузке).
- Убрать голый `except`: логировать факт промаха, чтобы деградация была видна.

### 1.6. `app.py` — файл нежизнеспособен
- Строка 170: `personal_recs.loc[rd['ncodpers'], ...]` — `rd` не определена (NameError замаскирован голым `except`, поэтому молча всегда `'0'`).
- Строка 216: `joblib.load(...)` при отсутствующем `import joblib`.
- Строки 166–167: `rf['renta'] = df['renta'].clip(...)` — в однострочный `rf` присваивается **вся колонка** из 13-миллионного `df` (аналогично `antiguedad`) — DataFrame раздувается до миллионов строк.
- Строка 119: бины возраста `[(2, 24), (25, 29), ...]` сдвинуты на 1 относительно обучения `(2, 23), (24, 28), ...` (см. вывод ячейки 12 `modeling.ipynb`).
- Строка 100: чтение полного `train_ver2.csv` ради генерации одного случайного профиля; предсказание делается не для входных данных, а для случайной строки (`random.choice`, без seed).
- Нет `if __name__ == '__main__':`, вся логика — на уровне модуля.

**Рекомендация:** удалить `app.py` (его роль — прототип инференса — уже покрывает `rec_sys.ipynb`), либо явно переименовать в `old/app_draft.py` (`.gitignore` уже содержит `old/`).

---

## 2. Корректность сервиса `app1.py` (без блокеров)

1. **Порядок операций не совпадает с обучением.** `age_interval` (строки 205–209) считается *до* заполнения пропусков `age` медианой (строка 220) → у клиента без возраста интервал будет NaN. Маппинг `fecha_alta`/`ult_fec_cli_1t` в интервалы (строки 210–212) идёт *до* заполнения пропусков модами (222–226) → `None` упадёт с TypeError внутри `find_interval` (сравнение даты с None), а заполнение модой уже «сырой» датой `'2014-07-28'` создаст значение, которого нет среди обучающих меток интервалов (OHE с `handle_unknown='ignore'` молча занулит признак). При обучении порядок был: fillna → бины. Привести порядок в соответствие.
2. **`find_interval` и жёстко зашитые интервалы.** Списки `fecha_alta_intervals`/`ult_fec_cli_1t_intervals` не отсортированы и **перекрываются на граничных днях** (`'2015-07-09'` входит в два интервала — сработает тот, что раньше в списке); при обучении `pd.qcut` давал полуоткрытые интервалы `(left, right]`. Границы в `app.py`/`rec_sys.ipynb` (`pd.cut` с `bin_edges1/2`) расходятся с `app1.py` на ±1 день (`2015-02-26` vs `2015-02-27`). Единый источник истины: сохранить интервальные таблицы из обучения в JSON (как уже сделано для `replacer.json`) и грузить их в сервисе.
3. **`async def predict` с тяжёлым CPU-bound кодом** (pandas + featuretools ≈ 136 мс средней латентности по вашему же отчёту) блокирует event loop: конкурентные запросы обслуживаются по очереди. Сменить на обычный `def` — uvicorn сам вынесет его в thread pool.
4. **`feature_engineering` на одной строке.** Агрегаты `mean_renta_by_pais_residencia`, `median_antiguedad_by_segmento` и т.п. при обучении считались по всей популяции, а на проде — по единственной строке запроса (среднее одного значения = само значение) → систематический сдвиг признаков, которые модель реально использует (`feature_importances.csv`: `num__renta_vs_country_mean`, `cat__median_antiguedad_by_segmento_140.0`). Плюс постройка EntitySet на каждый запрос — главный вклад в латентность. Минимально инвазивное решение: предрасчитать таблицы агрегатов при обучении (словари `{категория: mean/median}` → JSON), на проде делать обычный `map`; featuretools-DFS для одной строки оставить только для арифметических примитивов или заменить их 5 строками pandas.
5. **Нет валидации входа.** `data: dict` + обращения по ключам: отсутствие `indrel`/`indext` (строка 197) или любого «обязательного» поля → KeyError → 500 со стектрейсом. Описать Pydantic-модель запроса (черновик уже есть — JSON-пример в README/docstring), `Optional[...]` для необязательных полей; вернуть 422 с понятным сообщением.
6. **Нет служебных эндпоинтов.** README обещает Prometheus/Grafana, в коде нет ни `/metrics`, ни `/health`. Добавить минимум: `/health` (модель загружена, версия) и — если инфраструктура коммитится — `prometheus_client` со счётчиком запросов/гистограммой латентности.
7. **Наблюдаемость.** Отладочные `print('Got data')`, `print('Dilled nans with modes')` (опечатка), голые `except` (строки 160, 164, 225, 230). Завести `logging`, пробрасывать request-id, `except KeyError`/конкретные исключения с логом.
8. **Провенанс модели.** `modeling.ipynb` (ячейка 54) сохраняет `joblib.dump(final_model, 'fastapi/saved_model.pkl')` (директории `fastapi/` нет в репо). `rec_sys.ipynb` (ячейки 25–29) грузит **другой артефакт** — PyFunc-модель конкретного MLflow-рана (`runs:/97204034.../model`, run id захардкожен) и пересохраняет её в `data/saved_model.pkl` — именно её читает сервис через `joblib.load`. Такой pickle PyFunc-обёртки хрупок к версиям mlflow. Выбрать один способ экспорта (логично: `joblib.dump` самого sklearn-pipeline из обучающей ячейки), зафиксировать в README: файл модели, run id, дата, метрики.
9. **Константы предобработки дублируются и расходятся с обучением** (`medians`, `modes`, границы clip `26449.65/337117.17`, `1.0/207.0`): при переобучении они пересчитываются из данных, а в `app1.py` останутся старые. Сохранять их из обучающего запуска в `preprocessing_params.json` и грузить в сервисе.
10. Мелочи: `import datetime` посреди файла (строка ~82); неиспользуемый `original_df` в `feature_engineering`; парные `try/except` на drop колонок, где второй блок затирает результат первого; docstring с curl в конце файла дублирует README — оставить в одном месте; `prediction` возвращать вместе со списком классов/вероятностями полезнее для рекомендательного сервиса (`predict_proba` и топ-N, у модели уже есть `classes_`).

---

## 3. Содержание: ML-методология (`modeling.ipynb`, `eda.ipynb`, `rec_sys.ipynb`)

1. **Целевая переменная покрывает 3 продукта из 24** (ячейка 24): `lambda row: [i+1 for i in range(3) if row[products[i]] == 1 ...]`. Учитываются только `ind_ahor_fin_ult1`, `ind_aval_fin_ult1`, `ind_cco_fin_ult1`. Это подтверждается `classification_report.txt`: классы «1» и «2» имеют support = **1**, класс «3» — 6005 при 275 тыс. негативов. Фактически система отвечает на вопрос «купит ли клиент текущий счёт», а не «какие продукты порекомендовать». Решить и зафиксировать в README: либо это осознанное сужение (тогда переписать формулировку задачи и выводы), либо `range(3)` → `range(len(products))` / осмысленный шорт-лист из EDA (продукты с долей >5%).
2. **Выводы о качестве завышены.** Markdown-вывод заявляет «высокую эффективность», «Precision 0.97», «2.2% ошибочных рекомендаций». Это взвешенные метрики, их делает класс «0» (доля ~98%). У главного целевого класса «3»: **precision 0.15, recall 0.84, f1 0.26**; у классов 1/2 метрики вычислены по одному объекту и ничего не значат. Добавить в вывод per-class таблицу, macro-метрики, PR-кривую/PR-AUC для класса покупки; скорректировать формулировки («модель уверенно отсеивает незаинтересованных; точность попадания в покупку — 15%, это типичный уровень для cold-start cross-sell, дальнейшее улучшение — …»).
3. **Тюнинг по тестовой выборке** (ячейка 52): `objective` делает `pipeline.fit(X_train, y_train)`, а скорит по `X_test` — Optuna дважды использует тест; итоговый ROC-AUC 0.9685 = максимум из перебора. При этом `StratifiedKFold`, `cross_val_score`, `RandomizedSearchCV` импортированы, но не используются. Скорить по кросс-валидации на train (3 фолда достаточно), а `X_test` трогать один раз в конце.
4. **Утечки и порядок сплита.** Статистики (медианы, моды, квантили клиппинга 1%/96%, наборы редких категорий, возрастные бины из `calculate_age_intervals`, qcut-когорты дат) считаются по **всему** датасету до сплита; `feature_engineering(full_df)` с groupby-трансформами тоже вызывается до `train_test_split` (ячейка 53) — агрегаты содержат информацию о тестовых клиентах. Утечка умеренная, но это же корень train/serve-дублирования. Решение то же, что в п. 2.9: все статистики обучать на train, сериализовать, применять на проде.
5. **Дисбаланс классов.** Классы 1 и 2 с 2–3 объектами на весь датасет делают `class_weight='balanced'` бессмысленным для них, а стратифицированный сплит — хрупким. Слить редкие таргеты в `'other'` или исключить, зафиксировав решение.
6. **Optuna-настройка куцая:** 5 триалов / 20 минут, тюнятся только 3 параметра, при этом каждый триал дважды строит лес (отдельный RF на 100 деревьев внутри `SelectFromModel` + основной). Убрать `verbose=3` из классификатора (сотни строк «building tree N» в выводе ячейки 53), вынести `feature_selection` из триала или сократить `n_estimators` селектора, дать Optuna 20–30 триалов — это дешевле по времени, чем текущая конфигурация при той же пользе.
7. **ALS-блок без валидации.** Рекомендации ALS используются только как признак, сама качество ALS нигде не измеряется (нет precision@k/recall@k по `common_test`). Добавить 10–15 строк оценки — заодно появится содержательный бейзлайн, с которым честно сравнивать классификатор. И сохранить `personal_als.parquet` с индексом по `ncodpers` (см. 1.5). `user_map` строится по всем юзерам, включая встречающихся только в тесте — безвредно для признаков, но стоит комментария.
8. **`pl.DataFrame` без импорта polars** (ячейка 28, `get_recommendations_als`) — polars в notebook не импортируется, на чистом прогоне будет NameError. Заменить на `pd.DataFrame` (проект polars не использует).
9. **Молчаливая потеря строк:** цепочка `fillna('0')` → `dropna()` (ячейки 42–44) — залогировать, сколько клиентов и почему отваливается; `full_df['purchase'].astype(int)` уже после explode-строк — проверить, что таргет не «утекает» в строковый вид.
10. **Мёртвый код/«строительные леса»:** `remove_correlated_features` (определена, вызов закомментирован), `unix = {}` в ячейке 16 (заполняется, но используется только в 17 — объединить), `original_df` в `feature_engineering`, закомментированные блоки, ячейки `df['ncodpers']`, `!pip freeze |` (обрезанная команда). Убрать — ноутбуки станут заметно короче.
11. **`os.environ["AWS_ACCESS_KEY_ID"] = os.getenv(...)` до проверки на None** (`eda.ipynb` ячейка 44, `modeling.ipynb` ячейка 51): при отсутствии `.env` — TypeError вместо понятной ошибки; потом в следующей ячейке проверка делается повторно. Оставить один блок с проверкой. `TRACKING_SERVER_HOST = "127.0.0.1"` при docker-compose не сработает — вынести в `.env`.
12. **Хрупкое: `cool_names_dict = dict(zip(list(df.columns), items))`** — сопоставление русских имён колонкам по порядку; добавление/удаление колонки тихо всё сдвинет. Задать явным словарём или хранить пары «колонка; имя» в `columns.txt`.

---

## 4. Форма: качество кода (вне содержания)

1. **Дублирование.** `feature_engineering`/`manual_transformations` — 3 копии (`modeling` 48, `app1.py`, `app.py`); список продуктов — 5+ копий; `nums`/`cats` — 2 копии; верификационный пайплайн — `rec_sys.ipynb` ≈ `app.py`. Вынести в один модуль `preprocessing.py` (функции + константы), импортировать и в ноутбуке, и в сервисе. Это небольшая изоляция существующего кода, не переписывание.
2. **`cats[:-24]`** — позиционная магия «все категориальные кроме последних 24 продуктов»; перестановка колонки сломает молча. Хранить явные списки `PRODUCTS`, `CAT_COLS`, `NUM_COLS` в одном месте.
3. **Именование:** `rf` — это один клиент (single row), читается как «random forest»; `df`/`rf`/`cats` переиспользуются; `comon_train`; `make_heatmap(df, f, dict, products)` — параметр `dict` перекрывает встроенный тип; print-опечатки (`Dilled`, `columnds`, `numberic`).
4. **Строковые категории:** `rf[cat].astype('str')` после числовых колонок даёт `'1.0'`/`'0.0'` (видно в `feature_importances.csv`: `cat__total_products_5.0`), при этом `replacer.json` хранит ключ `'NaN'` строкой, а реальный `np.nan` ключом-строкой не совпадёт. Унифицировать: продуктовые флаги и `cod_prov` → `int` → `str`, пропуски → явная категория `'unknown'`.
5. **Примечание по ноутбукам:** `full_final_prj.ipynb` и `mle_final_prj2.ipynb` — это копии-«сшивки» всех ноутбуков (сдача курса с комментариями ревьюера). Любая правка канонических ноутбуков в них не попадёт. Задокументировать их роль в README (или убрать в `submissions/`), править только канонические файлы.
6. Линтер/форматтер не настроен; по существующему коду проблемы типовые (голые except, неиспользуемые импорты `requests`/`mlflow`/`load_dotenv` в `app1.py`/`app.py`, переизбыточные try/except). Достаточно прогнать `ruff check --select F,E` и поправить найденное — мелочь, дисциплинирует.

---

## 5. Тестирование (`test.ipynb`, отсутствие unit-тестов)

1. **«Нагрузочный» тест не нагружает** (ячейка 0): создаётся `ThreadPoolExecutor(max_workers=50)`, но запросы отправляются `send_request(url, df)` напрямую в цикле `while` — последовательно, ~1.8 RPS (в отчёте 92 запроса за 50 секунд успели). Executor не используется ни разу. Заменить на `executor.submit` + `as_completed` (или очередь фьючерсов с ограничением) — тогда 50 воркеров реально дадут нагрузку и p95/p99 обретут смысл.
2. Последняя ячейка дублирует тест с `max_time=505` — удалить или параметризовать (`max_time`, `workers`, `url` через переменные окружения/виджеты).
3. Полный `train_ver2.csv` читается, чтобы через `random.choice(df[col].unique())` собрать случайный профиль из несовместимых значений разных строк (возраст 116 + сегмент «UNIVERSITARIO»). Дешевле и честнее: взять 100 реальных строк датасета целиком (`df.sample(100, random_state=42).to_dict('records')`).
4. Критерий успеха — только HTTP 200. Добавить проверки схемы ответа (`prediction` — int из известного набора классов) и SLO по латентности, падение теста при нарушении.
5. **Unit/интеграционных тестов нет совсем.** Минимальный `pytest`-набор (без перестройки проекта):
   - golden-тest предобработки: один фиксированный вход → ожидаемый набор колонок/значений признаков;
   - `/predict`: happy path + битый запрос (нет `indrel`) → 422/понятная ошибка;
   - train/serve-согласованность: один и тот же клиент через ноутбучный пайплайн и через сервис → одинаковый вектор признаков (поймал бы почти все баги раздела 2);
   - smoke на артефакты: модель/`replacer.json`/`personal_als.parquet` грузятся, индекс parquet — `ncodpers`.

---

## 6. Документация

1. **README (помимо блокера 1.3):**
   - Множество опечаток: «килентов», «Необхоидмо», «Устраненик», «чиловых», «библиоетка», «с помощтью», «mlfkow», «править тестовые запросы» (→ «отправить»), «graphana/Graphana» (→ «Grafana»), «тетради modeling.ipynb» (→ «тетрадке»), «dэшборды» (→ «дашборды»), «анализа данных» (→ «анализ»).
   - Оформление: курсив `*команда*` → код-блоки ` ``` `; `'''` → ` ``` `; нумерация «1) 3) 4)»; убрать угловые скобки в `http://localhost:<8079>`; `![alt text]` → осмысленный alt.
   - Структура: добавить разделы «Структура репозитория» (особенно: зачем `app.py`, `app1.py`, 7 ноутбуков, `full_final_prj`/`mle_final_prj2`), «Как получить данные» (сейчас `data/train_ver2.csv` неоткуда взять: `loader.ipynb` тянет из S3 по кредам из `.env` — приложить `.env.example` с ключами `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, параметрами бакета/MLflow), «Результаты» — честная таблица метрик (в т.ч. per-class), «Ограничения» (таргет по 3 продуктам, статус инфраструктуры).
   - `export $(cat .env | xargs)` ломается о значениях с пробелами — заменить на `set -a; source .env; set +a`.
2. `recommendations_analysis.md` дублирует выводы EDA — дать на него ссылку из README вместо поддержки двух текстов; в самом файле поправить «Денежный средства» → «Денежные средства» (это же в `columns.txt`).
3. Docstrings функций в ноутбуках/`app1.py` местами расходятся с поведением (`feature_engineering` не «объединяет с оригинальными данными», а заменяет df на feature_matrix — комментарий `pd.concat` закомментирован: синхронизировать описание).
4. В комментариях `modeling.ipynb` typo «evens» (→ «events»), «колнка», «warings»; в `eda.ipynb` «верменных», «предоб��аботки» (битый символ), «обранружены», «десткий».

---

## 7. Гигиена репозитория

1. Ноутбучные outputs закоммичены (5+ МБ суммарно; html-таблицы, «building tree…»-логи). Для учебного проекта оставить читаемые ноутбуки нормально, но добавить `nbstripout`/pre-commit на будущее или хотя бы очистить лог-спам ячеек 27/53 в `modeling.ipynb`.
2. Артефакты в корне (`classification_report.txt`, `feature_importances.csv`, `replacer.json`, `columns.txt`, `image.png`, `load_test_report.*`) — при желании аккуратно переложить в `artifacts/` и поправить 3–4 ссылки (необязательно).
3. `rec_sys.ipynb` и `app.py` дублируют друг друга; после удаления `app.py` (п. 1.6) останется одна тетрадь для офлайн-проверки инференса.
4. `load_test_report.html` ссылается на `load_test_report.png` относительным путём — при переносах не забудьте пару.
5. `.gitignore` в порядке (кроме отсутствия `mlruns/`, `*.pkl`, `*.parquet` вне `data/` — добавить строкой).

---

## 8. План работ (приоритеты, трудоёмкость: S < 1 ч, M — часы, L — день+)

### P0 — «репозиторий собирается и сервис честный» — ✅ выполнено 15.09.2026
| # | Действие | Файлы | Статус |
|---|----------|-------|--------|
| 1 | Разрешить merge-конфликты | `requirements.txt`, `loader.ipynb`, `README.md` | ✅ маркеры убраны; `loader.ipynb` — валидный JSON; README переписан |
| 2 | `load_dotenv` → `python-dotenv`; добавить `fastapi`, `uvicorn`, `pyarrow`, `woodwork` | `requirements.txt` | ✅ `python-dotenv==1.0.1`, `fastapi==0.115.8`, `uvicorn==0.34.0`, `pyarrow==19.0.1`, `woodwork==0.31.0` |
| 3 | Исправить замену редких категорий (`.replace` вместо `map`) | `app1.py` | ✅ |
| 4 | Индекс `ncodpers` для ALS-lookup + лог промахов вместо голого except | `modeling.ipynb` (яг. 35), `app1.py` | ✅ parquet сохраняется с `set_index('ncodpers')`; в сервисе — fallback для старого формата файла + `logger.info` при промахе |
| 5 | Удалить/вынести сломанный `app.py` | `app.py` | ✅ файл удалён (остался в истории git) |
| 6 | Починить порядок операций в `predict` (fillna → бины) | `app1.py` | ✅ fillna медиан/мод выполняется до расчёта интервалов; заодно `print` → `logger.debug`, `errors='ignore'` при drop |
| 7 | Переписать README под фактическое состояние (app1, uvicorn, данные, `.env.example`) | `README.md`, новый `.env.example` | ✅ README переписан (структура репозитория, получение данных, честный статус инфраструктуры), `.env.example` добавлен |

Дополнительно в рамках P0: в `loader.ipynb` сохранение датасета приведено к `df.to_csv('data/train_ver2.csv', index=False)`.

### P1 — «методологическая честность и воспроизводимость» — ✅ выполнено 16.09.2026
| # | Действие | Файлы | Статус |
|---|----------|-------|--------|
| 8 | Решить судьбу таргета (`range(3)`) + отразить в README и выводах | `modeling.ipynb`, `README.md` | ✅ таргет — многоклассовый: шорт-лист продуктов (доля покупок 2015 ≥ 1%, не больше 12) плюс классы `0` (покупки не было) и `other` (редкий продукт); правило и состав шорт-листа описаны в README («Целевая переменная») и в выводах ноутбука |
| 9 | Optuna: скоринг по CV на train; тест — один раз в конце | `modeling.ipynb` (яч. 52–53) | ✅ `cross_val_roc_auc` считает ROC-AUC по `StratifiedKFold(3, shuffle, 42)` на train; тестовая выборка оценивается один раз после выбора гиперпараметров, `cv_roc_auc_mean` логируется в MLflow |
| 10 | Сериализовать все параметры предобработки из обучения (бины, медианы/моды, clip-границы, когорты дат, агрегаты) → единые JSON-артефакты для сервиса | `modeling.ipynb`, `app1.py` | ✅ `fastapi/preprocessing_params.json` (`medians`, `modes`, `clip_bounds`, `age_intervals`, `date_cohorts`, `aggregates`, `replacer`, `label_map`, `feature_columns`); `app1.py` читает файл, а при его отсутствии работает на legacy-константах и пишет предупреждение в лог |
| 11 | Честный отчёт о качестве: per-class + macro + PR-AUC; поправить markdown-выводы; метрики ALS (precision@k) | `modeling.ipynb`, `README.md` | ✅ `classification_report.txt` — per-class таблица, macro-/взвешенные метрики и PR-AUC с долей класса; `als_metrics.csv` — precision@k, recall@k, hit_rate; выводы ноутбука и разделы README «Проверка модели» / «Ограничения» переписаны |
| 12 | Слить редкие классы таргета; убрать `verbose=3`, мёртвый код, `pl.DataFrame` → `pd.DataFrame` | `modeling.ipynb` | ✅ классы с support меньше 100 сворачиваются в `other` (`MIN_CLASS_SUPPORT`); `verbose=3`, мёртвые ячейки (`comon_train`, `remove_correlated_features`) и `pl.DataFrame` удалены |
| 13 | `async def predict` → `def`; Pydantic-схема запроса; `/health` | `app1.py` | ✅ `def predict(profile: ClientProfile)` — обычная функция (внутри CPU-bound pandas/featuretools), ответ — `PredictionResponse` (`prediction`, `product`, `confidence`, `top_k`), добавлен `GET /health` |

**Условие вступления правок в силу:** код ноутбука и сервиса изменён, но артефакты
(`fastapi/saved_model.pkl`, `fastapi/preprocessing_params.json`, `als_metrics.csv`,
`classification_report.txt`) пересоздаются только при прогоне `modeling.ipynb` на реальных
данных — до переобучения в репозитории лежат артефакты и метрики прошлого запуска,
а `app1.py` для отсутствующего `preprocessing_params.json` работает на legacy-константах.
Проверка выполнена на синтетическом датасете (ноутбук проходит целиком, сервис корректно
работает на его артефактах); прогон на настоящем `data/train_ver2.csv` не выполнялся —
в окружении сборки нет ключей Kaggle и самого файла.

Дополнительно в рамках P1: `loader.ipynb` переведён с S3-бакета на загрузку датасета из
соревнования Kaggle (`KAGGLE_USERNAME`/`KAGGLE_KEY`, `kaggle==1.7.4.5` в `requirements.txt`,
шаблон `.env.example`); в `test.ipynb`, `eda.ipynb`, `rec_sys.ipynb` путь к датасету приведён
к `data/train_ver2.csv` (его сохраняет `loader.ipynb`), `.env` добавлен в `.gitignore`.


### P2 — «качество жизни и поддержка» — ✅ выполнено 16.09.2026
| # | Действие | Файлы | Статус |
|---|----------|-------|--------|
| 14 | Общий модуль `preprocessing.py` (функции + списки колонок + константы) — убрать 3 копии | `app1.py`, `modeling.ipynb`, `rec_sys.ipynb` | ✅ новый `preprocessing.py` (константы, `PreprocessingParams`, единый `prepare_features`/`feature_engineering`); `app1.py` — тонкая обёртка, ноутбуки импортируют из модуля |
| 15 | Нагрузочный тест: реально параллельные запросы, реальные профили, проверка схемы/SLO | `test.ipynb` | ✅ `executor.submit` + `as_completed`, `df.sample(100)` целиком, проверка схемы ответа и SLO (p95/success_rate, падение при нарушении), параметры через env |
| 16 | Минимальный `pytest`-набор (golden-test предобработки, `/predict`, train/serve-согласованность) | новый `tests/` | ✅ 35 тестов: unit-предобработка (golden, регрессии §1.4/§1.5/§2.1), API (happy path, 422/503, офлайн-паритет), smoke артефактов; `requirements-dev.txt`, конфиг в `pyproject.toml` |
| 17 | `ruff`-прогон (голые except, неиспользуемые импорты, имена); опечатки в markdown/комментариях | весь проект | ✅ `ruff check` (F, E) чист, включая ноутбуки; голые `except` убраны, опечатки (§6.4) и `columns.txt` исправлены |
| 18 | `/metrics` + закоммитить `Dockerfile`/compose/prometheus/grafana.json **или** убрать обещания из README | инфраструктурные файлы | ✅ `GET /metrics` (счётчики + гистограмма латентности) в `app1.py`; `Dockerfile`/compose/prometheus переписаны под `app1`, `graphana.json` → `grafana.json` с запросами под новые метрики, дубли (`old_prometheus.yaml`, `r.md`) удалены |
| 19 | Роли ноутбуков в README; `submissions/` для курсовых сшивок; `nbstripout` на будущее; `artifacts/` для артефактов | репозиторий | ✅ роли и порядок ноутбуков в README; сшивок в репо нет — переезд не нужен; `.gitattributes` + `nbstripout`; `.gitignore` дополнен (`mlruns/`, `*.pkl`, `*.parquet`); переезд в `artifacts/` отложен (опционально по §7.2, требует правок путей) |
| 20 | Один канонический экспорт модели + фиксация версии (run id, дата, метрики) в README | `modeling.ipynb`, `rec_sys.ipynb`, `README.md` | ✅ канон — `fastapi/saved_model.pkl` (`rec_sys.ipynb` переписан на него + `preprocessing`); `fastapi/model_version.json` (run `e200be8d…`, дата, метрики) пишется ячейкой экспорта; таблица версии в README |

Дополнительно в рамках P2 (найдено тестами/проверками):
- `fastapi/personal_als.parquet`: в файле были дубли `ncodpers` с NaN-строкой первой —
  дедуп `keep='first'` убивал все рекомендации; файл пересохранён (94 091 клиент,
  93 254 непустых), загрузчик и ячейка сохранения предпочитают непустые значения.
- `add_age_interval`: `pd.cut` без `include_lowest` давал NaN на минимальной границе —
  исправлено в `preprocessing.py` и в `modeling.ipynb` (яч. 13).

**Ожидаемый эффект:** после P0 проект устанавливается, сервис отдаёт корректные признаки (включая главный — персональную рекомендацию), а инструкции запуска соответствуют содержимому репозитория. После P1 метрики становятся честными и воспроизводимыми, а переобучение перестаёт ломать прод из-за разошедшихся констант. P2 закрепляет результат тестами и снижает цену будущих правок.

### P3 — «оставшиеся огрехи из README» — ✅ выполнено 16.09.2026

Пункты из раздела «Ограничения и известные проблемы» README, которые были заявлены как
«следующие шаги сверх P2» (статистики только на train, временное разбиение, перенос отчётов
в `artifacts/`), плюс заодно найденные дефекты.

| # | Действие | Файлы | Статус |
|---|----------|-------|--------|
| P3.1 | Статистики предобработки обучаются только на train (медианы, моды, квантили клиппинга, возрастные бины, когорты дат, наборы редких категорий, групповые агрегаты) | `preprocessing.py`, `modeling.ipynb` | ✅ `fit_preprocessing_params(df)` (все fit-статистики, перенесённые `calculate_age_intervals`/`create_time_cohorts` из ноутбука) + `apply_preprocessing(df, params)`; `prepare_features` переиспользует `apply_preprocessing` — train/serve путь един; агрегаты обучаются в `feature_engineering(X_train)` |
| P3.2 | Агрегаты по невиданным категориям: fallback `__default__` вместо NaN | `preprocessing.py` | ✅ в словари агрегатов при fit пишется ключ `__default__` (глобальная статистика train); `map` при apply/проде покрывает категории, отсутствовавшие в train, иначе RandomForest падал бы на NaN. Для старых артефактов без `__default__` — fallback через среднее значений словаря |
| P3.3 | Временное разбиение вместо стратифицированного | `preprocessing.py`, `modeling.ipynb` | ✅ `temporal_split(dates)`. ⚠️ Разбиение по **дате среза** (`fecha_dato`) экспериментально оказалось **вырожденным**: ~65% клиентов имеют последний срез 2015-12-28, а остаток — ушедшие раньше клиенты оттока, у которых «покупок 2016» нет априори → train из бывших клиентов с таргетом 0. Выбран осмысленный вариант: разбиение по **дате привлечения** (`fecha_alta`): train — привлечённые до cutoff (≈70%), test — новее. Сплит не зависит от исхода 2016-го, клиенты в одной строке, дата - настоящая временная ось. Зафиксировано в `preprocessing_params.json` (`split`), `model_version.json` и MLflow (`split_type=temporal_fecha_alta`) |
| P3.4 | Сворачивание редких категорий: строковое сравнение (устранение train/serve skew) | `preprocessing.py`, `tests` | ✅ обучение приводит колонки к str до подсчёта частот, а прод сравнивал float к строковым ключам — редкие `cod_prov`/`ind_nuevo` не сворачивались (OHE молча занулял). Теперь словари со строковыми ключами сравниваются в строковом домене; словари с числовыми ключами — в исходном. Тест заменён на `test_fold_rare_categories_matches_training_semantics` |
| P3.5 | Индексация ALS-матрицы (§3, п.7 фиксировал лишь отсутствие валидации ALS; индексацию заметили позже) | `modeling.ipynb` | ✅ csr-матрица строилась по строкам (0..N), а `recommend()` вызывался с `user_map`-индексами → рекомендации приписывались «соседнему» клиенту (off-by-one против user_map: сырые ncodpers 1..N против индексов 0..N-1). Исправлено: строки матрицы — `ncodpers.map(user_map)`. Числа в als_metrics до пересчёта относятся к старой индексации |
| P3.6 | Метрики ALS в двух скоупах | `modeling.ipynb` | ✅ `artifacts/als_metrics.csv` получил колонку `scope`: `common_users_2015_2016` (старый скоуп) и `all_users_2016` (все покупатели 2016-го, cold-start без рекомендаций → hits=0) — честная оценка системной качества |
| P3.7 | Дрейф-мониторинг признаков (PSI) | `drift.py`, `app1.py`, `preprocessing.py`, `tests` | ✅ `compute_drift_reference` (децили age/antiguedad/renta train) в `preprocessing_params.json`; `DriftMonitor` — потокобезопасное скользящее окно входных значений в сервисе; PSI на скрейпе → gauge `bank_recommender_drift_psi`; `GET /drift` — JSON-отчёт; `DRIFT_WINDOW_SIZE`/`DRIFT_MIN_SAMPLES`. Профили без значений не наблюдаются (медианы обучения «вымывали» бы сдвиг). Версия сервиса 1.3.0 |
| P3.8 | Алерты Prometheus | `fastapi/prometheus/alerts.yml`, `fastapi/prometheus/prometheus.yml`, `fastapi/docker-compose.yaml` | ✅ правила: простой (up==0), p95 > 2 с, доля 5xx > 1%, PSI-дрейф > 0.25. Alertmanager намеренно не добавлен (сработки видны в UI Prometheus); путь для уведомлений — в README |
| P3.9 | Нагрузочный тест как скрипт (можно из CI) | `load_test.py`, `test.ipynb` | ✅ вся логика — в `load_test.py` (env-параметры, SLO, код возврата 0/1, отчёты в `artifacts/`); `test.ipynb` — тонкая обёртка |
| P3.10 | Отчёты переехали в `artifacts/` | репозиторий | ✅ `classification_report.txt`, `feature_importances.csv`, `als_metrics.csv`, `load_test_report.html`/`.png`; пути обновлены в ноутбуках/скриптах/тестов/README |
| P3.11 | `requirements.txt` не резолвился | `requirements.txt` | ✅ `mlflow==2.7.1` требовал `numpy<2` и `pyarrow<14` при пинах `numpy==2.2.5`/`pyarrow==19.0.1` → ResolutionImpossible. Обновлено `mlflow==2.22.1` (numpy<3, pyarrow<20); пустой `pip install --dry-run -r requirements.txt` проходит |

Проверка: полный прогон `modeling.ipynb` на синтетическом датасете (~60k строк, 6.4k
клиентов, реалистичные правила покупок) с MLflow-сервером — ноутбук выполняется до конца,
артефакты обновляются, сервис поднимается на них и отвечает на `/predict`, `/drift`,
`/metrics`, короткий прогон `load_test.py` проходит. Тесты: 61 тест (`preprocessing`,
fit/apply/train-serve-паритет, `temporal_split`, дрейф, API) + `ruff check` — чисто.
Прогон на настоящем `data/train_ver2.csv` не выполнялся (в окружении нет ключей Kaggle
и файла данных): закоммиченные артефакты (`fastapi/*`, `artifacts/*`) остаются от
предыдущего реального запуска и обновятся после переобучения — модель и сервис
обратно совместимы (legacy-пути сохранены).
